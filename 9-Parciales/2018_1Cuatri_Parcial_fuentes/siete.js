function mostrar()
{
	var contador;
	var notas;
	var acumulador;
	var sexo;
	var cantidadvarones;

	contador=0;

	while(contador<5) 
	{
		contador=contador+1;
		notas=prompt("Ingrese su nota");
		sexo=prompt("Ingrese si es f o m");

		if (sexo!="f" && sexo!="m")
		{
			prompt("Ingrese si es f o m")
		}
		if (notas>-1 && notas<11)
		{
			notas=parseInt(notas);
			notas=prompt("Ingrese su nota")
			acumulador=acumulador+notas;

		}
		if (notas>5 && sexo!="m") 
		{

		}


		alert


	}




}
